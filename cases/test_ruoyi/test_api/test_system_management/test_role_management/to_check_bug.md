# 角色管理模块 - 疑似 BUG 记录

> 以下 BUG 均通过测试脚本执行日志分析发现，结合请求体、响应体进行判定。
> 分析日志批次：20260307_231xxx（最新执行）

---

## BUG-1: 角色显示顺序 roleSort 接受负数值 【待确认】

- **关联用例**: D04 (`test_add_role_sort_negative`)
- **复现步骤**: POST `/system/role`，请求体中 `"roleSort": -1`，其他字段合法
- **实际结果**: 系统返回 `{"msg": "操作成功", "code": 200}`，角色成功创建
- **预期结果**: 显示顺序应仅接受非负整数（≥0），负数应被拒绝返回 code=500
- **影响**: 负数排序值无业务意义，可能导致前端角色列表排序混乱
- **严重级别**: 低
- **BUG 类型**: 输入校验
- **脚本断言**: `check=[["$.code", "in", [200, 500]]]`（骑墙断言，ISS-1 问题已记录）
- **确认证据**: 日志 `test_role_sort_validation_20260307_231309.log`：请求体 `"roleSort": -1` → 响应 `{"msg": "操作成功", "code": 200}`

---

## BUG-2: 角色显示顺序 roleSort 接受小数值 【待确认】

- **关联用例**: D06 (`test_add_role_sort_decimal`)
- **复现步骤**: POST `/system/role`，请求体中 `"roleSort": 3.5`，其他字段合法
- **实际结果**: 系统返回 `{"msg": "操作成功", "code": 200}`，角色成功创建
- **预期结果**: roleSort 为整型排序字段，传入小数应拒绝（code=500），或截断为整数并确保存储一致
- **影响**: 小数排序值与 UI 展示不一致；若数据库字段为 int 类型，可能发生静默截断导致传入值与存储值不一致
- **严重级别**: 低
- **BUG 类型**: 输入校验
- **脚本断言**: `check=[["$.code", "in", [200, 500]]]`（骑墙断言，ISS-1 问题已记录）
- **确认证据**: 日志 `test_role_sort_validation_20260307_231309.log`：请求体 `"roleSort": 3.5` → 响应 `{"msg": "操作成功", "code": 200}`。注：脚本未查询存储值，无法确认是原样存储还是被截断。
