# 部门管理测试用例 (113个)

---

## A. 基础 CRUD (TC-A1~A6) — 已实现

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-A1 | 创建部门-正常流程 | 系统运行正常 | 1. POST创建部门(parentId=100, deptName, orderNum=1, status="0", leader, phone, email) 2. GET查询部门列表获取deptId | 1. 返回code=200, msg="操作成功" 2. 列表中包含新建部门 |
| TC-A2 | 查询部门列表 | 已创建测试部门 | 1. 创建部门 2. GET查询列表(deptName筛选) | 列表返回中包含新建部门, deptName/orderNum/status正确 |
| TC-A3 | 查询部门详情 | 已创建测试部门 | 1. 创建部门(含所有字段) 2. 查询列表获取deptId 3. GET查询详情 | 详情中deptName/orderNum/status/leader/phone/email均正确 |
| TC-A4 | 更新部门 | 已创建测试部门 | 1. 创建部门 2. PUT更新所有字段 3. 查询详情验证 | 所有字段更新成功, 详情返回新值 |
| TC-A5 | 删除部门 | 已创建测试部门 | 1. 创建部门 2. DELETE删除 | 返回code=200, msg="操作成功" |
| TC-A6 | 完整生命周期 | 无 | 1. 创建 2. 查详情 3. 更新 4. 查详情验证 5. 删除 | 每步均返回code=200 |

---

## B. 父子层级关系 (TC-B1~B5) — 已实现

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-B1 | 创建子部门 | 无 | 1. 创建父部门(parentId=100) 2. 创建子部门(parentId=父部门ID) 3. 查询子部门详情 | 子部门parentId等于父部门ID |
| TC-B2 | 多级层级(3层) | 无 | 1. 创建父→子→孙 3层 2. 查询孙子详情 | 孙子部门parentId等于子部门ID |
| TC-B3 | 删除有子部门的部门-应失败 | 父子部门 | 1. 删除父部门 | code!=200, msg包含"存在下级部门" |
| TC-B4 | 设置父部门为自身-应失败 | 已创建部门 | 1. PUT修改parentId=自身deptId | code!=200, msg包含"上级部门不能是自己" |
| TC-B5 | 排除查询 | 父子部门 | 1. GET排除查询(deptId=父) | 返回列表不包含父及其子部门 |

---

## C. 状态管理基础 (TC-C1~C3) — 已实现

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-C1 | 停用部门 | 已创建启用部门 | 1. PUT修改status="1" 2. 查询详情 | status变为"1" |
| TC-C2 | 停用有启用子部门的部门-应失败 | 父子部门均启用 | 1. 直接停用父部门 | code!=200, msg包含"该部门包含未停用的子部门" |
| TC-C3 | 在停用父部门下新增子部门-应失败 | 父部门已停用 | 1. 在停用父部门下创建子部门 | code!=200, msg包含"部门停用" |

---

## D. 字段校验 (TC-D1~D5) — 已实现

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-D1 | 同父部门下部门名称重复-应失败 | 已有同名部门 | 1. 创建重复名称部门 | code!=200, msg包含"部门名称已存在" |
| TC-D2 | 部门名称超过30字符-应失败 | 无 | 1. 创建deptName=40字符 | code!=200 |
| TC-D3 | 联系电话超过11位-应失败 | 已创建部门 | 1. 更新phone=14位数字 | code!=200 |
| TC-D4 | 邮箱格式非法-应失败 | 已创建部门 | 1. 更新email="invalid-email" | code!=200 |
| TC-D5 | 必填项缺失-应失败 | 无 | 1. 创建deptName="" | code!=200 |

---

## E. 查询边界 (TC-E1~E3) — 已实现

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-E1 | 查询不存在的部门 | 无 | 1. GET /system/dept/999999 | code=200 (返回空或null data) |
| TC-E2 | 部门名称含特殊字符 | 无 | 1. 创建含特殊字符名称 2. 查询列表 | 创建成功, 列表可查到 |
| TC-E3 | 无筛选条件返回全部 | 系统有部门数据 | 1. GET列表(无参数) | code=200, $.data存在 |

---

## F. 名称唯一性 (TC-F1~F5) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-F1 | 同名不同父部门-应成功 | 无 | 1. 在parentId=100下创建部门A 2. 创建父部门B(parentId=100) 3. 在父部门B下创建同名部门A | 步骤3成功, 同名部门可存在于不同父节点下 |
| TC-F2 | 修改名称为同父已有名称-应失败 | 同父下两个部门 | 1. 创建部门A(parentId=100) 2. 创建部门B(parentId=100) 3. PUT修改B的名称为A的名称 | code!=200, msg包含"部门名称已存在" |
| TC-F3 | 删除后重新创建同名-应成功 | 无 | 1. 创建部门A(parentId=100) 2. 删除部门A 3. 重新创建同名部门A(parentId=100) | 步骤3成功, code=200 |
| TC-F4 | 不同状态的同父同名-应失败 | 已有同名部门 | 1. 创建部门A(parentId=100, status=0) 2. 创建同名部门A(parentId=100, status=1) | 步骤2失败, 名称唯一性不受状态影响 |
| TC-F5 | 修改名称为自身原名-应成功 | 已创建部门 | 1. 创建部门A 2. PUT修改, deptName不变, 其他字段变更 | 更新成功, code=200 |

---

## G. 高级层级操作 (TC-G1~G10) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-G1 | 4层深度层级 | 无 | 1. 创建L1(parentId=100) 2. 创建L2(parentId=L1) 3. 创建L3(parentId=L2) 4. 创建L4(parentId=L3) 5. 查询L4详情 | L4的parentId=L3的deptId |
| TC-G2 | 移动部门到不同父节点 | 两个父部门+一个子部门 | 1. 创建父A、父B(parentId=100) 2. 创建子C(parentId=A) 3. PUT修改C的parentId=B 4. 查询C详情 | C的parentId变为B |
| TC-G3 | 创建时验证ancestors字段 | 无 | 1. 创建部门(parentId=100) 2. 查询详情检查ancestors | ancestors包含"100" |
| TC-G4 | 修改父节点后验证ancestors更新 | 父子部门 | 1. 创建父A(parentId=100) 2. 创建子B(parentId=A) 3. 创建父C(parentId=100) 4. 修改B的parentId=C 5. 查询B详情 | B的ancestors包含C的deptId |
| TC-G5 | 移动子部门到根级别 | 父子部门 | 1. 创建父(parentId=100) 2. 创建子(parentId=父) 3. 修改子的parentId=100 4. 查询子详情 | 子部门parentId=100 |
| TC-G6 | 同级创建多个兄弟部门 | 无 | 1. 创建父(parentId=100) 2. 在父下创建子A、子B、子C 3. 查询列表 | 3个子部门均存在, parentId相同 |
| TC-G7 | 设置父节点为子孙节点-应失败 | 父→子结构 | 1. 创建父→子 2. PUT修改父的parentId=子的deptId | code!=200, msg包含"上级部门不能是自己"或类似错误 |
| TC-G8 | 移动后验证子节点ancestors联动更新 | 父→子→孙 | 1. 创建父A→子B→孙C 2. 创建新父D 3. 修改B的parentId=D 4. 查询C详情 | C的ancestors包含D的deptId, 不再包含A |
| TC-G9 | 5层深度层级 | 无 | 1. 依次创建5层: L1→L2→L3→L4→L5 2. 查询L5详情 | L5创建成功, parentId=L4 |
| TC-G10 | 每层多子节点树结构 | 无 | 1. 创建父(parentId=100) 2. 在父下创建子A、子B 3. 在子A下创建孙A1、孙A2 4. 查询列表 | 所有部门均存在, 层级关系正确 |

---

## H. 状态转换 (TC-H1~H8) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-H1 | 启用已停用部门 | 已停用的叶子部门 | 1. 创建部门(status=0) 2. 停用(status=1) 3. 启用(status=0) 4. 查询详情 | status恢复为"0" |
| TC-H2 | 状态来回切换0→1→0 | 已创建部门 | 1. 创建(status=0) 2. 改为1 3. 验证 4. 改为0 5. 验证 | 每次切换后状态正确 |
| TC-H3 | 按状态=0筛选列表 | 有启用和停用部门 | 1. 创建部门A(status=0) 2. 创建部门B(status=0)并停用 3. lst_dept(status="0") | 列表包含A, 不包含B |
| TC-H4 | 按状态=1筛选列表 | 有启用和停用部门 | 1. 创建部门A(status=0) 2. 创建部门B(status=0)并停用 3. lst_dept(status="1") | 列表包含B, 不包含A |
| TC-H5 | 启用部门时祖先自动启用 | 父(停用)→子(停用) | 1. 创建父子, 全部停用 2. 启用子部门(status=0) 3. 查询父部门详情 | 父部门status自动变为"0" |
| TC-H6 | 停用叶子部门(无子部门) | 叶子部门 | 1. 创建叶子部门 2. 停用 | 成功, code=200 |
| TC-H7 | 先停用所有子部门再停用父部门 | 父子部门均启用 | 1. 创建父→子 2. 先停用子 3. 再停用父 | 两步均成功 |
| TC-H8 | 停用后查询详情验证状态 | 已停用部门 | 1. 创建部门 2. 停用 3. 查询详情 | 详情中status="1" |

---

## I. 更新场景 (TC-I1~I12) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-I1 | 仅更新部门名称 | 已创建部门 | 1. 创建部门 2. PUT仅修改deptName 3. 查询详情 | deptName变更, 其他字段不变 |
| TC-I2 | 仅更新负责人 | 已创建部门 | 1. 创建部门 2. PUT仅修改leader 3. 查询详情 | leader变更, 其他不变 |
| TC-I3 | 仅更新电话 | 已创建部门 | 1. 创建部门 2. PUT仅修改phone 3. 查询详情 | phone变更, 其他不变 |
| TC-I4 | 仅更新邮箱 | 已创建部门 | 1. 创建部门 2. PUT仅修改email 3. 查询详情 | email变更, 其他不变 |
| TC-I5 | 仅更新排序号 | 已创建部门 | 1. 创建部门(orderNum=1) 2. PUT修改orderNum=99 3. 查询详情 | orderNum变为99, 其他不变 |
| TC-I6 | 仅更新状态 | 已创建部门(叶子) | 1. 创建部门 2. PUT仅修改status=1 3. 查询详情 | status变为"1" |
| TC-I7 | 同时更新所有字段 | 已创建部门 | 1. 创建部门 2. PUT同时更新deptName/leader/phone/email/orderNum 3. 查询详情 | 所有字段全部更新正确 |
| TC-I8 | 更新为相同值(无实际变化) | 已创建部门 | 1. 创建部门 2. PUT传入完全相同的字段值 3. 查询详情 | code=200, 字段值不变 |
| TC-I9 | 更新名称为空-应失败 | 已创建部门 | 1. 创建部门 2. PUT修改deptName="" | code!=200 |
| TC-I10 | 更新时不传orderNum | 已创建部门 | 1. 创建部门 2. PUT修改只传deptName, orderNum使用默认值0 | 验证orderNum是否被更新为0 |
| TC-I11 | 清空可选字段 | 已创建部门(含leader/phone/email) | 1. 创建部门(含所有字段) 2. PUT清空leader/phone/email为空串 3. 查询详情 | leader/phone/email均为空 |
| TC-I12 | 更新后查询详情验证所有字段 | 已创建部门 | 1. 创建部门 2. 更新所有字段 3. 查询详情逐字段验证 | 每个字段值均与更新时一致 |

---

## J. 字段边界校验 (TC-J1~J15) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-J1 | deptName恰好30字符-应成功 | 无 | 1. 创建deptName=30字符的部门 | code=200, 创建成功 |
| TC-J2 | deptName 31字符-应失败 | 无 | 1. 创建deptName=31字符的部门 | code!=200 |
| TC-J3 | phone恰好11位-应成功 | 无 | 1. 创建部门(phone="13800138000") | code=200 |
| TC-J4 | phone 12位-应失败 | 无 | 1. 创建部门(phone="138001380001") | code!=200 |
| TC-J5 | phone含非数字字符-应失败 | 无 | 1. 创建部门(phone="1380013800a") | code!=200 |
| TC-J6 | email恰好50字符-应成功 | 无 | 1. 创建部门(email=50字符合法邮箱) | code=200 |
| TC-J7 | email 51字符-应失败 | 无 | 1. 创建部门(email=51字符) | code!=200 |
| TC-J8 | 多种非法邮箱格式 | 无 | 1. 创建部门(email="@example.com") 2. 创建(email="test@") 3. 创建(email="test @e.com") | 每次均code!=200 |
| TC-J9 | orderNum为0-应成功 | 无 | 1. 创建部门(orderNum=0) | code=200, 创建成功 |
| TC-J10 | orderNum为负数 | 无 | 1. 创建部门(orderNum=-1) | 验证系统行为(可能成功或失败) |
| TC-J11 | orderNum为极大值 | 无 | 1. 创建部门(orderNum=999999) | code=200, 创建成功 |
| TC-J12 | deptName全空格-应失败 | 无 | 1. 创建deptName="   " | code!=200或后端trim后视为空 |
| TC-J13 | deptName含前后空格 | 无 | 1. 创建deptName=" test_name " 2. 查询详情 | 验证是否trim处理 |
| TC-J14 | phone为空字符串-应成功 | 无 | 1. 创建部门(phone="") | code=200 |
| TC-J15 | email为空字符串-应成功 | 无 | 1. 创建部门(email="") | code=200 |

---

## K. 查询与筛选 (TC-K1~K10) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-K1 | 按精确名称筛选 | 已创建测试部门 | 1. 创建部门A 2. lst_dept(deptName=精确名称) | 返回中包含A |
| TC-K2 | 按模糊名称筛选 | 已创建包含关键字的部门 | 1. 创建部门"test_k2_xxx" 2. lst_dept(deptName="test_k2") | 返回中包含该部门 |
| TC-K3 | 按status=0筛选 | 有启用部门 | 1. 创建启用部门 2. lst_dept(status="0") | 返回中包含该部门 |
| TC-K4 | 按status=1筛选 | 有停用部门 | 1. 创建部门并停用 2. lst_dept(status="1") | 返回中包含该部门 |
| TC-K5 | 组合筛选(名称+status=0) | 有同名不同状态部门 | 1. 创建部门(status=0) 2. lst_dept(deptName=名称, status="0") | 返回仅包含启用的该部门 |
| TC-K6 | 组合筛选(名称+status=1) | 有停用部门 | 1. 创建部门并停用 2. lst_dept(deptName=名称, status="1") | 返回包含停用的该部门 |
| TC-K7 | 筛选结果为空 | 无 | 1. lst_dept(deptName="不存在的名称xyz") | code=200, $.data为空列表 |
| TC-K8 | 更新后立即查询验证 | 已创建部门 | 1. 创建部门 2. 更新名称 3. 用新名称查询 | 返回中包含更新后的部门 |
| TC-K9 | 查询停用部门详情 | 已停用部门 | 1. 创建并停用部门 2. lst_dept_detail | 详情返回正常, status="1" |
| TC-K10 | 排除查询验证子树完整排除 | 父→子→孙结构 | 1. 创建三层结构 2. lst_dept_exclude(deptId=父) | 返回列表不包含父、子、孙 |

---

## L. 删除场景 (TC-L1~L8) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-L1 | 删除叶子部门 | 无子部门的部门 | 1. 创建叶子部门 2. DELETE删除 | code=200, 删除成功 |
| TC-L2 | 删除后验证列表不包含 | 已创建部门 | 1. 创建部门 2. 删除 3. lst_dept(deptName=名称) | 列表中不再包含该部门 |
| TC-L3 | 有子部门不允许删除 | 父子部门 | 1. 创建父→子 2. 删除父 | code!=200, msg包含"存在下级部门" |
| TC-L4 | 有孙子部门时删子节点-应失败 | 父→子→孙 | 1. 创建三层 2. 删除子(有孙) | code!=200, msg包含"存在下级部门" |
| TC-L5 | 删除后重建同名 | 无 | 1. 创建部门A 2. 删除A 3. 创建同名部门A | 步骤3成功, code=200 |
| TC-L6 | 重复删除同一部门 | 已删除的部门 | 1. 创建并删除部门 2. 再次DELETE同一deptId | 验证系统行为(可能返回错误或200) |
| TC-L7 | 删除含用户的部门-应失败 | 部门下有用户 | 1. 创建部门 2. 创建用户关联到该部门 3. DELETE删除部门 | code!=200, msg包含"部门存在用户" |
| TC-L8 | 删除后排除列表不包含 | 已创建+删除部门 | 1. 创建部门 2. 记录deptId 3. 删除 4. lst_dept_exclude(其他deptId) | 已删除部门不在排除列表中 |

---

## M. 排序号 (TC-M1~M5) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-M1 | 同级多部门不同排序号 | 无 | 1. 创建A(orderNum=1) 2. 创建B(orderNum=2) 3. 创建C(orderNum=3) 4. 查询列表 | 三个部门均创建成功, orderNum分别为1,2,3 |
| TC-M2 | 同级多部门相同排序号 | 无 | 1. 创建A(orderNum=1) 2. 创建B(orderNum=1) 3. 查询列表 | 两个部门均创建成功, orderNum均为1 |
| TC-M3 | 修改排序号后验证 | 已创建部门 | 1. 创建部门(orderNum=1) 2. 修改orderNum=10 3. 查询详情 | orderNum变为10 |
| TC-M4 | orderNum为1的正常值 | 无 | 1. 创建部门(orderNum=1) 2. 查询详情 | orderNum=1 |
| TC-M5 | 排序号对列表顺序的影响 | 无 | 1. 创建A(orderNum=3) 2. 创建B(orderNum=1) 3. 创建C(orderNum=2) 4. 查询列表 | 验证列表按orderNum排序 |

---

## N. 集成场景 (TC-N1~N8) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-N1 | 完整树创建后从底向上删除 | 无 | 1. 创建父→子→孙 2. 删除孙 3. 删除子 4. 删除父 | 每步删除均成功 |
| TC-N2 | 创建后修改父节点验证树变化 | 无 | 1. 创建A(parentId=100) 2. 创建B(parentId=A) 3. 创建C(parentId=100) 4. 修改B的parentId=C 5. 查询B详情 | B的parentId=C |
| TC-N3 | 批量创建5+部门 | 无 | 1. 在parentId=100下连续创建5个部门 2. 查询列表 | 5个部门均存在 |
| TC-N4 | 链式创建root→d1→d2→d3→d4 | 无 | 1. 依次创建4层链 2. 查询d4详情 | d4的parentId=d3 |
| TC-N5 | 树修改后排除列表验证 | 多层树 | 1. 创建父→子A→孙A1 2. 创建子B(parentId=父) 3. lst_dept_exclude(deptId=子A) | 排除列表不包含子A和孙A1, 包含子B |
| TC-N6 | 子部门在父部门间移动 | 两父+一子 | 1. 创建A、B(parentId=100) 2. 创建C(parentId=A) 3. 移动C到B 4. 查询验证 | C的parentId从A变为B |
| TC-N7 | 全字段完整生命周期 | 无 | 1. 创建(含所有字段) 2. 查询验证所有字段 3. 更新所有字段 4. 查询验证更新 5. 删除 | 每步的每个字段均验证正确 |
| TC-N8 | 多分支树结构验证 | 无 | 1. 创建根→[A,B] 2. A→[A1,A2] 3. B→[B1] 4. 查询列表验证所有部门 | 6个子部门+1个根, 层级关系均正确 |

---

## O. 边界场景 (TC-O1~O10) — 新增

| 编号 | 标题 | 前置条件 | 测试步骤 | 预期结果 |
|------|------|----------|----------|----------|
| TC-O1 | 所有可选字段为空创建 | 无 | 1. 创建部门(只传deptName, parentId, orderNum) | code=200, leader/phone/email为空 |
| TC-O2 | deptName最大长度创建 | 无 | 1. 创建deptName=30字符 2. 查询详情 | 创建成功, 名称完整保存 |
| TC-O3 | 最小有效数据创建 | 无 | 1. 创建(deptName="a", parentId=100, orderNum=0) | code=200 |
| TC-O4 | leader含特殊字符 | 无 | 1. 创建部门(leader="张三@#$%") 2. 查询详情 | 创建成功, leader正确保存 |
| TC-O5 | phone恰好11位创建 | 无 | 1. 创建部门(phone="13800138000") 2. 查询详情 | phone正确保存 |
| TC-O6 | email恰好50字符创建 | 无 | 1. 构造50字符合法email 2. 创建部门 3. 查询详情 | email正确保存 |
| TC-O7 | 更新时清空所有可选字段 | 已创建部门(含全字段) | 1. 创建(含leader/phone/email) 2. 更新为全空串 3. 查询详情 | 可选字段均被清空 |
| TC-O8 | deptName含中英文混合 | 无 | 1. 创建deptName="测试Dept部门01" 2. 查询详情 | 名称完整保存 |
| TC-O9 | parentId=100顶级部门创建 | 无 | 1. 创建(parentId=100) 2. 查询详情 | parentId=100, 创建成功 |
| TC-O10 | 查询超大deptId | 无 | 1. lst_dept_detail(deptId=9999999999) | 接口不报错, code=200 |
